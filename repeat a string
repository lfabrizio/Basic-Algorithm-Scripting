function repeatStringNumTimes(str, num) {
// empty var
  var final = "";
  //if statement
  if (num < 0) return "";
  // for loop 
  for (var i = 0; i < num; i++){
    final += str;
  }

  
  return final;
}


repeatStringNumTimes("abc", 3);
